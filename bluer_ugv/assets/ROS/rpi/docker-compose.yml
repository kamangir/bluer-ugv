version: "3.9"

services:
  ros:
    build: .
    container_name: bluer_ugv_ros_rpi
    network_mode: host
    privileged: true
    stdin_open: true
    tty: true
    volumes:
      - /home/pi/git:/root/git
