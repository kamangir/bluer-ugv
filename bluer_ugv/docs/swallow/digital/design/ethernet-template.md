title:::

🔥

on `rangin.front` (`arzhang3-front`),

```bash
@swallow \
	ethernet \
	test \
	- \
	--is_server 1 \
	--server_name 0.0.0.0
```

🔥

on `rangin.top` (`rangin-top2`),

```bash
@swallow \
	ethernet \
	test \
	- \
	--is_server 0 \
	--server_name arzhang3-front.local
```