# swallow: digital: design: computer: naming

- `<ugv-name>` or `<ugv-name>-front`: a [swallow computer](../../../../../../../bluer-sbc/bluer_sbc/docs/swallow) that watches the front and controls the motors.
- `<ugv-name>-back`: a [swallow-head computer](../../../../../../../bluer-sbc/bluer_sbc/docs/swallow-head) that watches the back.
- 2 x `<ugv-name>-top`: two [swallow-head computer](../../../../../../../bluer-sbc/bluer_sbc/docs/swallow-head) that watch the sides (`-left` and `-right`).
